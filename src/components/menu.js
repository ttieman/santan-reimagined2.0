import React from "react";
import { Hero, Navbar, Card, Tabs, Container, Tile, Image, Block, Columns, Sizes } from "react-bulma-components";

import 'bulma/css/bulma.min.css';

const Menu = () => {
    return (
        <div>
          <p>hello</p>  
        </div>
    );
};

export default Menu;